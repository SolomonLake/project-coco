{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "f(){ ./start-cloud-functions.sh $1 ;};f",
    "deploy": "npm run build && gcloud functions deploy index --runtime nodejs10 --trigger-http --env-vars-file ../.prod-env.yaml",
    "watch": "npm-watch start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "watch": {
    "start": {
      "patterns": [
        "*"
      ],
      "extensions": "ts"
    }
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^1.2.1",
    "firebase-admin": "^8.5.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@firebase/firestore-types": "^1.5.0",
    "@types/node-fetch": "^2.5.0",
    "typescript": "^3.6.2",
    "npx": "^10.2.0",
    "npm-watch": "^0.6.0"
  }
}
