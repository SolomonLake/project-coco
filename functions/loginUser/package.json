{
  "name": "example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm install && npm run build && gcloud functions deploy loginUser --runtime nodejs10 --trigger-http --env-vars-file ../.prod-env.yaml"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "watch": {
    "start": {
      "patterns": [
        "*"
      ],
      "extensions": "ts"
    }
  },
  "dependencies": {
    "@google-cloud/functions-framework": "^1.2.1",
    "firebase-admin": "^8.5.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "@firebase/firestore-types": "^1.5.0",
    "@types/node-fetch": "^2.5.0",
    "typescript": "^3.6.2"
  }
}
